-- 039: структурные свойства-связи «Родители» и «Потомки» на корневом типе
-- (0.8.1, требование fcdf78f9 «Свойства „Родители“ и „Потомки“ объявлены на
-- корневом типе и наследуются»).
--
-- Структурные (нетипизированные) связи выражаются двумя свойствами-связями на
-- корневом типе мысли «основной тип»:
--   * «Родители» — направление `in`  (владелец — цель ребра);
--   * «Потомки»  — направление `out` (владелец — источник ребра).
-- Тип связи у них отсутствует (`structural = true` в config, `link_type_id`
-- нет) — проекция строится по нетипизированным рёбрам (`type_id IS NULL`).
--
-- Имя хранится в реестре (в отличие от типизированного свойства-связи, где имя
-- вычисляется из типа связи по направлению). Свойства наследуются всеми типами
-- от корневого; обязательными НЕ объявляются (у корневой мысли родителей нет).
--
-- Id свойства фиксированы, чтобы агент и миграции могли адресовать их, не
-- разрешая по имени; уникальность имени (name_key, layer_id) страхует от
-- двойного вставления при повторном прогоне.

INSERT OR IGNORE INTO properties
  (id, name, name_key, value_type, config, description, created_at, updated_at)
VALUES
  ('00000000-0000-4000-8000-0000000000c1', 'Родители', type_name_key('Родители'),
   'link', '{"structural":true,"direction":"in"}',
   'Структурная связь «родитель»: нетипизированные рёбра, у которых эта мысль — цель.',
   strftime('%Y-%m-%dT%H:%M:%SZ', 'now'), strftime('%Y-%m-%dT%H:%M:%SZ', 'now')),
  ('00000000-0000-4000-8000-0000000000c2', 'Потомки', type_name_key('Потомки'),
   'link', '{"structural":true,"direction":"out"}',
   'Структурная связь «потомок»: нетипизированные рёбра, у которых эта мысль — источник.',
   strftime('%Y-%m-%dT%H:%M:%SZ', 'now'), strftime('%Y-%m-%dT%H:%M:%SZ', 'now'));

-- Привязки к корневому типу мысли (id из миграции 021). Обязательность не
-- ставится; позиции идут после прочих свойств корневого типа.
INSERT OR IGNORE INTO type_properties
  (id, owner_type, owner_id, property_id, required, position)
VALUES
  ('00000000-0000-4000-8000-0000000000c3', 'thought_type',
   '00000000-0000-4000-8000-000000000001',
   '00000000-0000-4000-8000-0000000000c1', 0, 0),
  ('00000000-0000-4000-8000-0000000000c4', 'thought_type',
   '00000000-0000-4000-8000-000000000001',
   '00000000-0000-4000-8000-0000000000c2', 0, 1);
