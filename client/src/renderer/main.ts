/**
 * Renderer entry point.
 *
 * Phase G1 ships only a static placeholder screen (“ETN — подключение к серверу”).
 * The real UI (canvas, editor, onboarding form H2) arrives in phase H; the typed
 * `window.etn` IPC surface is introduced in G7. Nothing to execute yet.
 */
export {};
