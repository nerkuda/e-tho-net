-- 006: tabs.layer_id — слой изменений как свойство таба (S11, 13-layers.md §10.3).
-- Слой сессии сервера один на клиента (ключ (user, client_id)), поэтому таб
-- хранит выбранный им слой локально; при активации таба рендерер переключает
-- серверную сессию на layer_id таба (NULL — основа).

ALTER TABLE tabs ADD COLUMN layer_id TEXT;
