-- 008: tab.activity_state — персистентный фильтр вида «События»
-- (задача f27809d0 «Вид workspace «События» в клиенте», элемент UI 8cd9ad55).
-- Колонка опциональна (NULL = фильтр ещё не сохранялся). Добавление через
-- ALTER TABLE: действующие табы продолжают работать без миграции данных.
ALTER TABLE tabs ADD COLUMN activity_state TEXT;
