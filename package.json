{
  "name": "etn",
  "version": "0.1.0",
  "private": true,
  "description": "ETN — The Endless Thought Network. Self-hosted graph-of-thoughts application.",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm -w @etn/shared run build",
    "dev:server": "npm -w @etn/server run dev",
    "dev:client": "npm -w @etn/client run dev",
    "build": "npm run build:shared && npm -w @etn/server run build && npm -w @etn/client run build",
    "typecheck": "npm -w @etn/server run typecheck && npm -w @etn/client run typecheck && npm -w @etn/shared run typecheck",
    "test": "npm -w @etn/server test && npm -w @etn/client test"
  },
  "engines": {
    "node": ">=20"
  }
}
