# ETN — The Endless Thought Network

Бесплатный self-hosted «цифровой мозг»: заметки, идеи, задачи и файлы живут не
в папках, а в виде направленного графа связанных мыслей, который можно
листать, как разум — от общего к частному и обратно. Вдохновлён TheBrain, но
не копирует его, а развивает собственные методики работы с информацией — свой
сервер, свои данные, никаких подписок и облаков.

![CI](https://github.com/nerkuda/e-tho-net/actions/workflows/ci.yml/badge.svg) ![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)

![demo-gif](docs/images/ETN-1.gif)

> ⚠️ **Перед обновлением до 0.8.x — обязательно сделайте резервную копию базы!**
>
> В версии 0.8.1 **связи между мыслями становятся свойствами** — это большое внутреннее изменение, которое затрагивает базу данных. Программа сама перестроит данные при первом запуске, но **отката назад нет**: если что-то пойдёт не так, восстановить базу можно будет только из резервной копии.
>
> **Что нужно сделать перед обновлением:**
>
> 1. Остановите сервер.
> 2. **Скопируйте папку с базами данных целиком** (в каждой сети это файл `data.db`) в безопасное место. Это ваша страховка — без неё обновляться не рекомендуется.
> 3. Обновите сервер и клиент **вместе** — старая версия клиента не совместима с новым сервером, по отдельности они работать не будут.
> 4. Запустите сервер. Он сам применит все изменения к базе. При больших базах это может занять время — следите за логами.
> 5. Откройте свою сеть и убедитесь, что мысли и связи на месте. Если что-то потерялось, проверьте свойства мыслей, отборы и фильтры — возможно, они не учитывают новые настройки по умолчанию. При необходимости восстановите базу из копии и сообщите разработчикам.
>
> **После обновления.** Если вы подключаете к сети AI-агентов через MCP,
> проверьте их инструкции, скиллы и промпты: порядок работы со связями
> изменился. Связь теперь создаётся заполнением свойства (направление задано
> в самом свойстве), отдельного вызова создания связи больше нет, а
> `thought_ref`-свойства переведены в свойства-связи. Устаревшие рецепты
> агентов нужно поправить — иначе они будут обращаться к снятым операциям.
>
> Подробности — в [`docs/install-server.md`](docs/install-server.md) §8 и в `CHANGELOG.md` (раздел 0.8.1).

## Ключевые возможности

- 🧠 **Мысли связаны, а не разложены по папкам.** Заметка может быть связана с
  любым числом других — вверх и вниз, — поэтому одна и та же идея спокойно
  живёт сразу в нескольких контекстах. Карта показывает мысль в фокусе и её
  окружение: от общего к частному — один клик.
- 📝 **Внутри мысли — текст, файлы и история.** К каждой мысли — описание,
  вложения, картинки и лента датированных записей, которую можно использовать 
  как хронику размышлений или отчетов. 
  Текст — в Markdown, со ссылками на другие мысли и живым предпросмотром.
- 👁️ **Просмотры без потери контекста.** Зажми Ctrl и наведи курсор: текст,
  вложения, связи, датированные записи показываются во всплывающем окошке.
  Читаешь вглубь по ссылкам, ничего не открывая и не теряя текущую мысль.
- 🔍 **Поиск, который находит всё.** Ищет по названиям, заметкам, датированным
  записям и связям и наглядно показывает местонахождение каждой найденной фразы.
- 🏷️ **База, которая знает, что у неё внутри.** Задай свои типы — человек,
  книга, проект, источник — и поля для каждого: книге — автора и год,
  источнику — ссылку. Заметки перестают быть свалкой: по базе можно
  фильтровать по любому полю.
- 🎯 **Живые подборки под рукой.** Для типа мысли настраиваются подборки —
  «Главы в работе», «Источники по теме», — и кнопки для них появляются прямо
  на карте. Настраивается один раз для типа, а показывает то, что относится к
  открытой мысли.
- 🗂️ **Слои правок поверх базы.** Заведи слой и меняй в нем содержимое базы. 
  Он перекрывает только измененные тобой элементы, и не влияет на основные данные, 
  пока ты не сольёшь слой в основу. Не понравились изменения — слой можно 
  удалить бесследно.
- 🗑️ **Ничего не теряется случайно.** Мысль или связь не удалится, если на ней
  что-то держится, — ETN покажет, где она используется. Помеченное лежит в
  корзине и возвращается обратно одним движением.
- 👥 **Совместная работа в реальном времени.** Несколько человек правят одну 
  базу одновременно — изменения появляются у всех сразу, без перезагрузок, в том 
  числе и со слоями изменений. И никаких «а у меня старая версия».
- 🔒 **Твои данные — твоя собственность.** ETN живёт там, где скажешь: на твоём
  компьютере в домашней сети или на твоём сервере в облаке. Никаких подписок за 
  доступ к своим записям. Приложение — под Windows, macOS и Linux, бесплатное, 
  с открытым исходным кодом.
- 🤖 **ИИ-помощник, который уже знает твою базу.** Подключи ассистента (Claude,
  Opencode, ZCode и другие) через встроенный MCP-сервер — он сможет искать в
  твоих записях, дописывать их и связывать между собой, не переспрашивая
  контекст.

## Статус

Активная разработка функциональности. Планирование работ и контроль за выполнением
перенесены в мыслесеть мейнтейнера; закрытые фазы `docs/workplan/` оставлены для 
истории.

## Спецификации

Полный комплект проектных спецификаций (архитектура, модель данных, REST API,
real-time, MCP, аутентификация, клиент, UI, сценарии, словарь, настройки) — в
каталоге [`docs/`](docs/). Начать знакомство с [`docs/README.md`](docs/README.md).

> Внимание! Спецификации в каталоге [`docs/`](docs/) устарели и будут обновлены 
> из мыслесетей мейнтейнера после реализации соответствующей фичи (генерация 
> документов).


## Стек

- **Сервер:** Node.js 20+ (**рекомендуется 22 LTS** — для него у better-sqlite3 есть готовая prebuilt-binary; на Node 24 потребуется Python в PATH для компиляции), TypeScript, Fastify, better-sqlite3, WebSocket.
- **Клиент:** Electron, TypeScript.
- **Хранилище:** SQLite — одна `_system.db` + `networks/<id>/data.db` на сеть.
- **MCP:** `@modelcontextprotocol/sdk`.

## Структура монорепо

```
etn/
├── docs/      # спецификации и план работ
├── server/    # серверное приложение + CLI (etn init)
├── client/    # десктоп-клиент на Electron
├── shared/    # общие типы (DTO, константы, протоколы real-time)
└── package.json
```

## Разработка

```bash
npm install              # установить зависимости всех workspaces
npm run dev:server       # запуск сервера в режиме разработки
npm run dev:client       # запуск клиента
npm run typecheck        # проверка типов во всех workspaces
npm test                 # все тесты
```

## Установка

- **Сервер** — Docker (`docker compose up -d`) или Node.js 22 из исходников;
  подробно — [`docs/install-server.md`](docs/install-server.md).
- **Клиент** — готовые установщики (Windows/macOS/Linux) в
  [релизах](https://github.com/nerkuda/e-tho-net/releases); из исходников —
  [`docs/install-client.md`](docs/install-client.md).

## Лицензия

Проект распространяется под [MIT License](LICENSE).

## Создано с помощью ИИ

Этот проект полностью (на 100%) написан с помощью нескольких LLM и AI-агента
[ZCode](https://zcode.z.ai) — настольной агентной среды разработки.


---

## Техпроекты

- [ ] Мобильные прилложения
- [ ] Развитие прав доступа
- [ ] Язык сложных запросов
- [ ] Ежедневник и развитие хронологических заметок
- [ ] "Точки сборки"
- [ ] Развитие Markdown-редактора
- [ ] Переключатель видов список/таблица/канбан/карта
- [ ] Перевод диалогов на vue
- [ ] Генератор документации
- [ ] Общий граф знаний поверх нескольких мыслесетей
- [X] Фильтрация типов связей на карте мыслей
- [X] Свойства и связи: единый список и единый редактор
- [X] Единая модель связей: типизированные рёбра как свойства
- [X] Динамические списки для типов мыслей с помощью отборов
- [X] Унификация работы со свойствами
- [X] Слои изменений
- [X] Совместная работа: авторство, захват и события
- [X] Ревизия MCP-инструментов
- [X] Управление разработкой с помощью мыслесети
- [X] Markdown редактор с динамическим предпросмотром
- [X] Управление вложениями, иконки
- [X] MVP (фазы A-Q)
